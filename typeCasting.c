#include <stdio.h>
int main(){
    int b = 52;
    float a = 54;
    printf("The value of a is : %d", (int) a); // This is how we can conver the type of values.
    printf("The value of b is : %f", (float) b);
    return 0;
}