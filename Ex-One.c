#include <stdio.h>
int main(){
    int a, b;
    b = 10;
    printf("Enter the value of A: ");
    scanf("%d", &a); // Taking Input From User To Multiply With (b).
    printf("The value of A is: %d", b*a); // Here I'm Calculating.
    return 0; // I Don't Get This (return 0;)
}